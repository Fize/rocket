---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: managedclusters.storage.rocket.io
spec:
  group: storage.rocket.io
  names:
    kind: ManagedCluster
    listKind: ManagedClusterList
    plural: managedclusters
    shortNames:
    - mcls
    singular: managedcluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: STATE
      type: string
    - jsonPath: .status.kubernetesVersion
      name: VERSION
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedCluster is the Schema for the managed clusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedClusterSpec defines the desired state of Cluster
            properties:
              addons:
                description: Addons specifies the list of addons to enable for this
                  cluster
                items:
                  description: ClusterAddon defines the desired state of an addon
                  properties:
                    config:
                      additionalProperties:
                        type: string
                      description: Config is the configuration for the addon
                      type: object
                    enabled:
                      description: Enabled specifies whether the addon is enabled
                      type: boolean
                    name:
                      description: Name is the name of the addon
                      type: string
                  required:
                  - name
                  type: object
                type: array
              apiServer:
                description: |-
                  APIServer is the API server URL of the cluster
                  This field is optional and used for direct connectivity mode
                type: string
              connectionMode:
                default: Hub
                description: ConnectionMode specifies how the cluster connects to
                  the manager
                enum:
                - Hub
                - Edge
                type: string
              secretRef:
                description: |-
                  SecretRef is a reference to the secret containing cluster credentials
                  The secret should contain: caData, certData, keyData, and token
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              taints:
                description: Taints are the taints applied to the cluster for scheduling
                items:
                  description: |-
                    The node this Taint is attached to has the "effect" on
                    any pod that does not tolerate the Taint.
                  properties:
                    effect:
                      description: |-
                        Required. The effect of the taint on pods
                        that do not tolerate the taint.
                        Valid effects are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: Required. The taint key to be applied to a node.
                      type: string
                    timeAdded:
                      description: |-
                        TimeAdded represents the time at which the taint was added.
                        It is only written for NoExecute taints.
                      format: date-time
                      type: string
                    value:
                      description: The taint value corresponding to the taint key.
                      type: string
                  required:
                  - effect
                  - key
                  type: object
                type: array
            type: object
          status:
            description: ManagedClusterStatus defines the observed state of Cluster
            properties:
              addonStatus:
                description: AddonStatus represents the status of addons
                items:
                  description: AddonStatus defines the observed state of an addon
                  properties:
                    message:
                      description: Message is the detailed message
                      type: string
                    name:
                      description: Name is the name of the addon
                      type: string
                    state:
                      description: State is the state of the addon
                      type: string
                  required:
                  - name
                  - state
                  type: object
                type: array
              agentVersion:
                description: AgentVersion is the version of the rocket agent
                type: string
              apiServerURL:
                description: APIServerURL is the url of the child cluster apiserver
                type: string
              conditions:
                description: Conditions represents the latest available observations
                  of the cluster's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                description: ID is the cluster ID (may be set by the cloud provider
                  or agent)
                type: string
              kubernetesVersion:
                description: KubernetesVersion is the version of the kubernetes cluster
                type: string
              lastKeepAliveTime:
                description: LastKeepAliveTime is the last time the agent sent a heartbeat
                format: date-time
                type: string
              nodeSummary:
                description: NodeSummary represents the summary of nodes status in
                  the member cluster
                items:
                  description: NodeSummary represents the summary of nodes status
                    in a specific cluster
                  properties:
                    name:
                      description: Name is the name of the resource pool
                      type: string
                    readyNum:
                      description: ReadyNum is the number of ready nodes in the cluster
                      type: integer
                    totalNum:
                      description: TotalNum is the total number of nodes in the cluster
                      type: integer
                  type: object
                type: array
              resourceSummary:
                description: ResourceSummary represents the summary of resources in
                  the member cluster
                items:
                  description: ResourceSummary represents the summary of resources
                    in the member cluster
                  properties:
                    allocatable:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: |-
                        Allocatable represents the resources of a cluster that are available for scheduling
                        Total amount of allocatable resources on all nodes
                      type: object
                    allocated:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: |-
                        Allocated represents the resources of a cluster that have been scheduled
                        Total amount of required resources of all Pods that have been scheduled to nodes
                      type: object
                    allocating:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: |-
                        Allocating represents the resources of a cluster that are pending for scheduling
                        Total amount of required resources of all Pods that are waiting for scheduling
                      type: object
                    name:
                      description: Name is the name of the resource pool
                      type: string
                  type: object
                type: array
              state:
                default: Pending
                description: State represents the current state of the cluster
                enum:
                - Pending
                - Ready
                - Offline
                - Rejected
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
